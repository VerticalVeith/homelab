apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: immich
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`immich.buergerhoff.com`)
      services:
        - name: immich-server
          port: 2283